class Notes {
    constructor(x, y, height, width,speed) {
    this.x = x;
    this.y = y;
    this.height = height;
    this.width = width;
    this.speed = speed;
    }
}

export {Notes}

// class Notes {
//     constructor(x, y, height, width, speed) {
//     this.x = Math.random () * canvas.width;
//     this.y = 0;
//     this.height = 24;
//     this.width = 24;
//     this.speed = Math.random () * 2 + 1
//     }
// }